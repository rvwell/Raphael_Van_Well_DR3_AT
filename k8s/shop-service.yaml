apiVersion: v1
kind: Service
metadata:
  name: shop-service
  namespace: dr3-at
  labels:
    app: shop-service
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: shop-service

